fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
reportlab>=4.1.0
httpx>=0.27.0
pandas>=2.0.0
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0
shap>=0.44.0
numpy>=1.26.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
python-multipart>=0.0.5
# AI + PDF intake (ported from ClaimGuard pre-pay pipeline)
anthropic>=0.104,<1.0
pdfplumber>=0.11.4
fpdf2>=2.7,<3.0
markdown>=3.5,<4.0  # Markdown → HTML for LLM document generation (markdown_to_pdf)
# Connectors: SFTP transport (optional; only loaded when an SFTP connector runs)
paramiko>=3.0,<4.0
# MCP server (server/mcp_server.py) — exposes the assistant to Claude Desktop
mcp>=1.2.0
# penguin-ai-sdk installed separately per SDK docs

# Browser automation (provider portal upload)
playwright>=1.45,<2.0
